import React from "react";

const ProductCard = ({ name, price, description, image }) => {
  return (
    <div className="max-w-sm rounded-2xl shadow-md p-4 bg-white">
      <img
        src={image}
        alt={name}
        className="w-full h-48 object-cover rounded-xl"
      />
      <h2 className="text-xl font-bold mt-3">{name}</h2>
      <p className="text-gray-600 mt-1">{description}</p>
      <p className="text-lg font-semibold text-green-700 mt-2">₹{price}</p>
      <button className="mt-3 w-full bg-blue-600 text-white px-4 py-2 rounded-xl hover:bg-blue-700">
        Add to Cart
      </button>
    </div>
  );
};

export default ProductCard;
